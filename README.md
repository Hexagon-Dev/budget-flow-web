# budget-flow-web

Frontend for budget control system. Allows to monitor operations with money, analytics and much more.  
Uses PrimeVue as component library.

### Project setup
```shell
npm install
```

Copy .env.example to .env and fill it with your data if needed.

### Run project
```shell
npm run dev
```